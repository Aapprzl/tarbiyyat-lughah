rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // TEMPORARY: Allow all access for migration
    // TODO: Change back to secure rules after migration complete
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
